# quantum
Two cryptography demos in Python: a Tkinter app for generating large probable primes (Miller-Rabin), and a Qiskit implementation of Shor's algorithm that breaks a toy RSA key via quantum phase estimation. 
